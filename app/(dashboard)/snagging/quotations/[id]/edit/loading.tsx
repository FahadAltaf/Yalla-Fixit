import { WizardSkeleton } from "@/components/dashboard/snagging/route-skeletons";

/** The quotation form while it loads. */
export default function EditQuotationLoading() {
  return <WizardSkeleton />;
}
