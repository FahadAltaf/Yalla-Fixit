import { ReportSkeleton } from "@/components/dashboard/snagging/route-skeletons";

/** The report while it loads, shaped like the A4 sheet. */
export default function ReportLoading() {
  return <ReportSkeleton />;
}
