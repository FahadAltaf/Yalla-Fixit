import { DesnagSkeleton } from "@/components/dashboard/snagging/route-skeletons";

/** The de-snag round builder while it loads. */
export default function DesnagLoading() {
  return <DesnagSkeleton />;
}
